# PyTorch (CUDA 11.3)
torch==1.11.0+cu113
torchvision==0.12.0+cu113
torchaudio==0.11.0
# Core dependencies
h5py
opencv-python
protobuf
av
einops
ruamel.yaml
timm
joblib
pandas
matplotlib
cython
scipy
transformers==4.24.0
numpy==1.23.5
# Additional tools
pycocotools
wandb
# Note: After installing requirements, you need to:
# 1. Build MultiScaleDeformableAttention: cd models/ops && python setup.py build install
# 2. Install torch-dwt: git clone https://github.com/KeKsBoTer/torch-dwt && cd torch-dwt && pip install -e .